# TODO for `proc_profiler`

- Test under FreeBSD and GNU/Linux.
- Docs and tests.
- Dynamic tracer load: Interface
- Per-thread CPU (not available through `psutil`) and per-thread status (available)
- Aggregation and re-sampling of processes
